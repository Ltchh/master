#define _CRT_SECURE_NO_WARNINGS 1#include<stdio.h>//**********新单词**************// address  地址// values   值// student	学生// clothes	品牌// aize		尺寸// coat		外套// trousers 裤子//***********结构体概念*********//数组		相同类型元素的集合//结构体	一些值的集合;但是值的类型可以不相同struct clothes {	char brand[10];	int coat_size;	int trousers;};struct student {		//steuct:关键字   student:名    ----(类型 <列如:int arr=0 >)	//成员变量	char name[10];	//姓名	int age;		//年龄	char id[10];	//学号	struct clothes clo;};void print1(struct student stu) {	printf("????:%s\n????:%d\n???:%s\n??????:%s\n???????:%d\n???????:%d\n", stu.name, stu.age, stu.id, stu.clo.brand, stu.clo.coat_size, stu.clo.trousers);	/*printf("????:%s\n????:%d\n???:%s\n??????:%s\n???????:%d\n???????:%d\n)", stu.name, stu.age, stu.id, stu.clo.brand, stu.clo.coat_size, stu.clo.trousers);*/}void print0() {	printf("---------------------\n");}void print2(struct student* stu) {	printf("????:%s\n????:%d\n???:%s\n??????:%s\n???????:%d\n???????:%d\n", stu->name, stu->age, stu->id, stu->clo.brand, stu->clo.coat_size, stu->clo.trousers);}int main() {	struct student stu = { "王明阳",65,"20220216",{"美特斯",175,27} };										//-----(对象)		//printf("姓名:%s,年龄:%d,学号:%s,衣服:(品牌:%s,上衣尺寸:%d,裤子尺寸:%d)", stu.name, stu.age, stu.id, stu.clo.brand, stu.clo.coat_size, stu.clo.trousers);		// 传值: . 传址:  ->		print1(stu);//传值调用	print0();	print2(&stu);//传址调用	return 0;} 